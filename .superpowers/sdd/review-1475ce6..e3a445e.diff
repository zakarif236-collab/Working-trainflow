# Review package: 1475ce6..e3a445e

## Commits
e3a445e feat: add countdown beep sounds to CueService

## Files changed
 lib/services/cue_service.dart | 350 +++++++++++++++++++++++++++++++++++++++---  1 file changed, 330 insertions(+), 20 deletions(-)

## Diff
diff --git a/lib/services/cue_service.dart b/lib/services/cue_service.dart index 8a9e254..ed3a634 100644 --- a/lib/services/cue_service.dart +++ b/lib/services/cue_service.dart @@ -1,112 +1,422 @@  import 'dart:async'; +import 'dart:convert'; +import 'dart:io'; +import 'dart:math' as math; +  import 'package:flutter/foundation.dart';  import 'package:flutter_tts/flutter_tts.dart'; +import 'package:my_app/config/gemini_config.dart'; +import 'package:just_audio/just_audio.dart'; +import 'package:path_provider/path_provider.dart';    class CueService { -  CueService() : _tts = _supportsVoiceCues ? FlutterTts() : null; +  CueService();      static bool get _supportsVoiceCues =>        !kIsWeb && defaultTargetPlatform != TargetPlatform.windows;   -  final FlutterTts? _tts; +  FlutterTts? _tts; +  AudioPlayer? _cachePlayer; +  bool _engineInitAttempted = false;    double _volume = 1.0;    double _speechRate = 0.52; +  final Map<String, String> _promptCache = <String, String>{}; +  Future<void>? _cacheInitFuture; +  String? _cacheDirectoryPath;      bool get supportsVoiceCues => _supportsVoiceCues; +  bool get geminiVoiceOverConfigured => GeminiConfig.isConfigured;      Future<void> updateSettings({double? volume, double? speechRate}) async {      if (volume != null) {        _volume = volume.clamp(0.0, 1.0);      }      if (speechRate != null) {        _speechRate = speechRate.clamp(0.2, 0.8);      }   +    await _ensureEnginesReady();      if (_tts == null) {        return;      }   -    final tts = _tts; +    final tts = _tts!; +    final player = _cachePlayer;        try {        await tts.setVolume(_volume);        await tts.setSpeechRate(_speechRate); +      if (player != null) { +        await player.setVolume(_volume); +      }      } catch (e) {        throw CueServiceException('Unable to apply voice cue settings: $e');      }    }   -  Future<void> announcePhase(String phaseLabel) async { -    if (_tts == null) { +  Future<void> announceExercise( +    String exerciseText, { +    bool shouldSpeak = true, +  }) async { +    final text = exerciseText.trim(); +    if (text.isEmpty) {        return;      } -    final tts = _tts; -    await _configureTts(); -    await tts.speak('$phaseLabel phase'); +    await _speakPrompt('Next exercise: $text.', shouldSpeak: shouldSpeak); +  } + +  Future<void> announceRest({bool shouldSpeak = true}) async { +    await _speakPrompt('Next rest. Take a break.', shouldSpeak: shouldSpeak); +  } + +  Future<void> announcePhase(String phaseLabel) async { +    await announceExercise(phaseLabel);    }      Future<void> speakCount(int seconds, {bool shouldSpeak = true}) async { -    // Play voice only if enabled and seconds > 0 -    if (!shouldSpeak || seconds < 1 || _tts == null) { +    if (!shouldSpeak || seconds < 1) {        return;      }   -    final tts = _tts; -    await _configureTts(); -    await tts.speak('$seconds'); +    await _speakPrompt('$seconds', shouldSpeak: shouldSpeak);    }      Future<void> playPhaseCompletionBeep() async {      // Beep signal will be provided by haptic feedback in the UI layer      // This method is kept for API compatibility      return;    }      Future<void> playWorkoutCompletionBeep() async {      // Beep signal will be provided by haptic feedback in the UI layer      // This method is kept for API compatibility      return;    }   +  Future<void> playCountdownBeep() async { +    await _playGeneratedBeep( +      frequencyHz: 880, +      durationMs: 100, +      volume: 0.7, +    ); +  } + +  Future<void> playCountdownFinalBeep() async { +    await _playGeneratedBeep( +      frequencyHz: 1320, +      durationMs: 200, +      volume: 1.0, +    ); +  } +    Future<void> announceCompletion() async { -    if (_tts == null) { -      return; -    } -    final tts = _tts; -    await _configureTts(); -    await tts.speak('Workout complete. Great job.'); +    await _speakPrompt('Workout complete. Great job.', shouldSpeak: true);    }      Future<void> _configureTts() async { +    await _ensureEnginesReady();      if (_tts == null) {        return;      }   -    final tts = _tts; +    final tts = _tts!;        try {        await tts.setVolume(_volume);        await tts.setPitch(1.05);        await tts.setSpeechRate(_speechRate);        await tts.awaitSpeakCompletion(true);      } catch (e) {        throw CueServiceException('Voice cues are unavailable on this device: $e');      }    }   +  Future<void> _speakPrompt(String text, {required bool shouldSpeak}) async { +    final trimmed = text.trim(); +    await _ensureEnginesReady(); +    if (!shouldSpeak || trimmed.isEmpty || _tts == null) { +      return; +    } + +    final tts = _tts!; +    await _configureTts(); + +    try { +      await _ensureCacheReady(); +      final cacheKey = _buildPromptKey(trimmed); +      final cachedPath = _promptCache[cacheKey]; +      if (cachedPath != null && await File(cachedPath).exists()) { +        await _playCachedFile(cachedPath); +        return; +      } + +      final generatedPath = await _synthesizePromptToFile(trimmed, cacheKey); +      if (generatedPath != null) { +        _promptCache[cacheKey] = generatedPath; +        await _persistPromptCache(); +        await _playCachedFile(generatedPath); +        return; +      } +    } catch (_) { +      // Fallback to direct TTS when cache generation or playback is unavailable. +    } + +    try { +      await tts.speak(trimmed); +    } catch (e) { +      throw CueServiceException('Voice cues are unavailable on this device: $e'); +    } +  } + +  Future<void> _ensureCacheReady() async { +    if (_cacheDirectoryPath != null) { +      return; +    } + +    final pending = _cacheInitFuture; +    if (pending != null) { +      await pending; +      return; +    } + +    final initFuture = _initializeCache(); +    _cacheInitFuture = initFuture; +    await initFuture; +  } + +  Future<void> _ensureEnginesReady() async { +    if (!_supportsVoiceCues) { +      return; +    } + +    if (_engineInitAttempted) { +      return; +    } + +    _engineInitAttempted = true; + +    try { +      _tts = FlutterTts(); +    } catch (_) { +      _tts = null; +    } + +    try { +      _cachePlayer = AudioPlayer(); +    } catch (_) { +      _cachePlayer = null; +    } +  } + +  Future<void> _initializeCache() async { +    final directory = await getApplicationDocumentsDirectory(); +    final cacheDirectory = Directory( +      '${directory.path}${Platform.pathSeparator}voice_prompt_cache', +    ); +    if (!await cacheDirectory.exists()) { +      await cacheDirectory.create(recursive: true); +    } + +    _cacheDirectoryPath = cacheDirectory.path; + +    final indexFile = File( +      '${cacheDirectory.path}${Platform.pathSeparator}prompt_index.json', +    ); +    if (!await indexFile.exists()) { +      return; +    } + +    try { +      final raw = await indexFile.readAsString(); +      if (raw.trim().isEmpty) { +        return; +      } + +      final decoded = jsonDecode(raw); +      if (decoded is! Map) { +        return; +      } + +      for (final entry in decoded.entries) { +        final key = entry.key.toString(); +        final value = entry.value.toString(); +        if (key.isEmpty || value.isEmpty) { +          continue; +        } +        if (await File(value).exists()) { +          _promptCache[key] = value; +        } +      } +    } catch (_) { +      // Corrupt cache index should not break voice cues. +    } +  } + +  Future<void> _persistPromptCache() async { +    final cachePath = _cacheDirectoryPath; +    if (cachePath == null) { +      return; +    } + +    final indexFile = File( +      '$cachePath${Platform.pathSeparator}prompt_index.json', +    ); +    await indexFile.writeAsString(jsonEncode(_promptCache)); +  } + +  Future<String?> _synthesizePromptToFile(String prompt, String cacheKey) async { +    final cachePath = _cacheDirectoryPath; +    final tts = _tts; +    if (cachePath == null || tts == null) { +      return null; +    } + +    final extension = defaultTargetPlatform == TargetPlatform.iOS ? 'caf' : 'wav'; +    final outputPath = '$cachePath${Platform.pathSeparator}$cacheKey.$extension'; + +    try { +      await tts.synthesizeToFile(prompt, outputPath, true); +      final outputFile = File(outputPath); +      if (await outputFile.exists()) { +        return outputPath; +      } +    } catch (_) { +      return null; +    } + +    return null; +  } + +  Future<void> _playCachedFile(String filePath) async { +    final player = _cachePlayer; +    if (player == null) { +      return; +    } +    await player.setFilePath(filePath); +    await player.play(); +  } + +  String _buildPromptKey(String text) { +    var hash = 2166136261; +    final bytes = utf8.encode(text.trim().toLowerCase()); +    for (final value in bytes) { +      hash ^= value; +      hash = (hash * 16777619) & 0xffffffff; +    } +    return hash.toUnsigned(32).toRadixString(16).padLeft(8, '0'); +  } + +  Future<void> _playGeneratedBeep({ +    required double frequencyHz, +    required int durationMs, +    required double volume, +  }) async { +    await _ensureEnginesReady(); +    final player = _cachePlayer; +    if (player == null) { +      return; +    } + +    final sampleRate = 22050; +    final numSamples = (sampleRate * durationMs / 1000).round(); +    final data = _generateSineWaveWav( +      frequencyHz: frequencyHz, +      sampleRate: sampleRate, +      numSamples: numSamples, +      volume: volume, +    ); + +    final tempDir = await getTemporaryDirectory(); +    final file = File( +      '${tempDir.path}${Platform.pathSeparator}beep_${frequencyHz.round()}_$durationMs.wav', +    ); +    await file.writeAsBytes(data, flush: true); + +    try { +      await player.setFilePath(file.path); +      await player.play(); +    } catch (_) { +      // Beep playback is best-effort +    } +  } + +  Uint8List _generateSineWaveWav({ +    required double frequencyHz, +    required int sampleRate, +    required int numSamples, +    required double volume, +  }) { +    final amplitude = (volume * 32767).round().clamp(-32768, 32767); +    final byteRate = sampleRate * 1 * 2; +    final dataSize = numSamples * 2; +    final fileSize = 36 + dataSize; + +    final buffer = ByteData(44 + dataSize); +    var offset = 0; + +    // RIFF header +    buffer.setUint8(offset, 0x52); offset++; // R +    buffer.setUint8(offset, 0x49); offset++; // I +    buffer.setUint8(offset, 0x46); offset++; // F +    buffer.setUint8(offset, 0x46); offset++; // F +    buffer.setUint32(offset, fileSize, Endian.little); offset += 4; +    buffer.setUint8(offset, 0x57); offset++; // W +    buffer.setUint8(offset, 0x41); offset++; // A +    buffer.setUint8(offset, 0x56); offset++; // V +    buffer.setUint8(offset, 0x45); offset++; // E + +    // fmt chunk +    buffer.setUint8(offset, 0x66); offset++; // f +    buffer.setUint8(offset, 0x6D); offset++; // m +    buffer.setUint8(offset, 0x74); offset++; // t +    buffer.setUint8(offset, 0x20); offset++; // (space) +    buffer.setUint32(offset, 16, Endian.little); offset += 4; // chunk size +    buffer.setUint16(offset, 1, Endian.little); offset += 2; // PCM +    buffer.setUint16(offset, 1, Endian.little); offset += 2; // mono +    buffer.setUint32(offset, sampleRate, Endian.little); offset += 4; +    buffer.setUint32(offset, byteRate, Endian.little); offset += 4; +    buffer.setUint16(offset, 2, Endian.little); offset += 2; // block align +    buffer.setUint16(offset, 16, Endian.little); offset += 2; // bits per sample + +    // data chunk +    buffer.setUint8(offset, 0x64); offset++; // d +    buffer.setUint8(offset, 0x61); offset++; // a +    buffer.setUint8(offset, 0x74); offset++; // t +    buffer.setUint8(offset, 0x61); offset++; // a +    buffer.setUint32(offset, dataSize, Endian.little); offset += 4; + +    // Generate sine wave samples with fade-out +    final twoPiFOverSr = 2.0 * 3.141592653589793 * frequencyHz / sampleRate; +    for (var i = 0; i < numSamples; i++) { +      final fadeOut = (1.0 - i / numSamples); +      final sample = (amplitude * math.sin(twoPiFOverSr * i) * fadeOut).round(); +      buffer.setInt16(offset, sample.clamp(-32768, 32767), Endian.little); +      offset += 2; +    } + +    final bytes = Uint8List(44 + dataSize); +    final byteBuffer = buffer.buffer.asByteData(); +    for (var i = 0; i < bytes.length; i++) { +      bytes[i] = byteBuffer.getUint8(i); +    } +    return bytes; +  } +    Future<void> stop() async {      await _tts?.stop(); +    await _cachePlayer?.stop();    }      Future<void> dispose() async {      await _tts?.stop(); +    await _cachePlayer?.dispose();    }  }    class CueServiceException implements Exception {    const CueServiceException(this.message);      final String message;      @override    String toString() => message;
