# flutter_timezone_example_agp8

Demonstrates how to use the flutter_timezone plugin from an Android app
built with Android Gradle Plugin 8.x.

The primary `example/` app targets AGP 9. This app is kept around to verify
that the plugin remains compatible with AGP 8.x.
